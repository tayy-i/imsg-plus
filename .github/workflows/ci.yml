name: CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  portable:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Check portable source
        run: ./scripts/check-portable
